# Elite Finish Auto — Website Changes (2026-06-05)

Full summary of this session. New pages: `book.html`, `gallery.html`, and six
blog posts under `blog/`. Stylesheet cache version is currently `?v=25`.

---

## Booking system — `book.html` (NEW)
3-step booking flow wired to **Formspree** (`https://formspree.io/f/mzzrdqvr`):
1. **Vehicle** — Sedan / SUV / 7-Seater·Pickup·Full-size, with animated blue
   vehicle graphics (float + wheels spin on hover).
2. **Package + add-ons** — correct prices per vehicle, selectable package,
   optional add-on checkboxes; "Save $20" shown under the price.
3. **Details** — Preferred day, time, Name, Phone, Address (**all required**),
   plus Notes box with backup-times placeholder.
- **Estimated total** shown before submit (with "+" = final price after assessment).
- Submits via AJAX → **thank-you screen** with Gallery / TikTok / Instagram buttons.
- Every "Book" button site-wide opens `book.html`.
- Top-nav blue button changed from "Call" to **"Book now"** on all pages.

## Gallery — `gallery.html` (NEW)
- Uniform 4:3 grid (3/2/1 columns by screen size); auto-loads
  `images/gallery-1.jpg … gallery-30.jpg`, missing numbers skipped.
- Currently 15 photos; header is a single line "A few images from our recent work".

## Blog — 6 full articles (NEW, under `blog/`)
All have H1, headings, internal links, `BlogPosting` schema, OG/Twitter, canonical:
1. How to Protect Your Car from London Winter Salt
2. Mobile vs In-Shop Car Detailing: Which Is Better?
3. How Often Should You Detail Your Car?
4. What's Included in a Full Car Detail?
5. Why Paint Protection Matters in Ontario
6. How to Remove Pet Hair from Car Interiors
- Linked from the blog index ("Read the guide →") and added to the sitemap.
- Added a **"Tips"** link to the top nav on every page (next to Gallery).

## Images added
- Home **neighbourhood cards** now use photos (London-Masonville/FoxHollow/
  Sunningdale/HydePark.webp) with a dark overlay for legible labels.
- Home **intro** is two-column: text + `images/intro.jpg` (IMG_3512).
- Each **location page** feature box uses a real photo:
  Masonville (IMG_4730, rotated), Fox Hollow (IMG_4324), Sunningdale (IMG_4835),
  Hyde Park (IMG_3946). All optimized; originals kept in the folder.
- June promo popup image → `images/june-offer.jpg`.

## Content edits
- **Odour Removal** add-on price set to **$30**.
- **Iron Decon & Clay Bar** description: "leaving a smooth, glass-like finish…".
- Pricing footnote: final price assessed in person or via photos.
- **Sunningdale**: removed the word "quiet"; replaced "leather-specific" with
  "all our products are safe and tested on all materials".

## Footer
- Hours updated to **Mon–Sun · 8am–10pm** sitewide.
- Added clickable **social icons**: Instagram, TikTok, YouTube, Facebook.

## June promo popup
- Shows **once per visit** (session-based), closes via ✕ / backdrop / Escape,
  image links to booking.

## Mobile + nav fixes
- Tightened hero/section spacing, full-width hero buttons, **centered text** on
  mobile, and fixed left-side cropping (`overflow-x: clip`).
- Fixed the **sticky top nav** so it stays visible while scrolling.
- Mobile dropdown menu now **closes when a link is tapped**.

## SEO overhaul
- **Favicon** (blue-star, 32/180 + apple-touch) on every page.
- **Open Graph share image** `images/og-image.jpg` (1200×630 branded card) +
  **Twitter Card** tags on every page.
- **Canonical** URLs on all pages.
- **Structured data**: fixed opening hours (Mon–Sun 08:00–22:00); added
  `logo`, `sameAs` (Instagram, TikTok, YouTube, Facebook, **Yelp, Yellow Pages,
  Bing Maps**), `aggregateRating` (5.0, **70** reviews) and 3 Review entries on
  the homepage; `sameAs` on all location pages. All JSON-LD validated.
- Sharpened titles to include "North London & London, Ontario".
- **sitemap.xml** updated and `robots.txt` points to it.

## Later-session polish & SEO
- **FAQ schema** (`FAQPage`) added to the homepage from the existing FAQ Q&As.
- **Contact / service-area page** — new `contact.html` (phone, email, hours,
  service areas, London map embed, `ContactPage` schema); linked from every
  footer "Contact" and added to the sitemap (**now 14 URLs**).
- **Citations added to schema** sitewide: **Yelp, Yellow Pages, Bing Maps** in
  `sameAs` (homepage + location pages).
- **Performance**: image dimensions + `loading="lazy"`/`decoding="async"` on
  intro, about, gallery, popup images and the logo; anchor `scroll-margin-top`
  so sticky-nav doesn't cover jumped-to sections.
- **June popup auto-expires after June 30, 2026** (won't show in July).
- Moved leftover large `IMG_*` originals into `images/originals/` (kept as
  backup, declutters the served folder). Verified zero broken image links.

## Header simplified
- Removed the four neighbourhood links from the top nav; replaced with a single
  **"Service Areas"** link (jumps to the homepage areas section, `#areas`).
- Neighbourhoods still shown in the homepage area cards + every footer.
- Every page now has an identical header: **Service Areas · Pricing · Gallery ·
  Tips · Book now**.

## Intro photo
- The "Premium & convenient" Bentley photo (`images/intro.jpg`) was re-cropped
  to **1000×500 (2:1 landscape)** and displays wide/short next to the text.

Stylesheet cache is currently `?v=34`.

---

## Open / next steps (off-site = biggest ranking levers)
- **Google Search Console**: verify the site and **submit `sitemap.xml`**, then
  Request Indexing on key pages. Repeat in **Bing Webmaster Tools**.
- **Google Business Profile**: confirm category, hours (Mon–Sun 8am–10pm),
  website link, and photos.
- Keep earning **Google reviews**; build a few local backlinks over time.
- When the host is ready: add the **Search Console verification** meta tag and
  any **Google Ads conversion** tracking.
- Write the remaining placeholder-free blog over time; optional favicon `.ico`.
